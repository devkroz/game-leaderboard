services:
  - type: web
    name: game-leaderboard-server
    runtime: node
    env: node
    buildCommand: cd server && npm install && npm run build
    startCommand: cd server && npm start
    plan: free
    healthCheckPath: /health